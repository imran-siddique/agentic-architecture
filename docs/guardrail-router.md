# Moved

This document was merged into [Routing before reasoning](./patterns/routing.md).

The Inference Trap, the Compute-to-Lookup Ratio, and the Guardrail Router
described one pattern from three angles: the failure, the ratio it produces,
and the component that enforces it. They are now one document, with the
implementation detail from all three retained.

This stub exists so links published before the merge still land somewhere
useful. It will be removed once the links have aged out.
