# Moved

This document was merged into [Silent execution](./patterns/silent-execution.md).

The Headless Agent, The Silent Swarm, and The Mute Agent described one pattern
from three distances: the message format between components, the topology that
separates the component holding language from the components holding tools, and
the individual worker that returns nothing rather than guessing. They are now
one document, with the implementation detail from all three retained.

This stub exists so links published before the merge still land somewhere
useful. It will be removed once those links have aged out.
