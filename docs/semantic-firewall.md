# Moved

This document was merged into [Grounded context](./patterns/grounded-context.md).

Multidimensional Knowledge Graphs, The Semantic Firewall, and Recursive
Ontologies described one pattern in three stages: build a structure you can
filter deterministically, enforce it on the way out, and keep it current from
failure signals. They are now one document, with the implementation detail from
all three retained and arranged by stage.

This stub exists so links published before the merge still land somewhere
useful. It will be removed once those links have aged out.
